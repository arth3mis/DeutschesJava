//package b;
zufuhr djava.util.*;

offen klasse test {
	offen statisch leer haupt(Strippe[] argumente) {
		System.raus.druckz("moin");
		versuch {} fang (Ausnahme a) {}
		versuch {
			wenn (Mathe.qwrz(4) == 2) {
				zurück;
			}
		} fang (Ausnahme a) {
		}
	}
}

/*

main;      haupt

Math.pow;  Mathe.hoch
Math.sqrt; Mathe.qwrz

öffentlich / offen ???
privatisiert

zu includen in txt:
--instanceof
--yield
--continue
--assert
--throw
--throws
--record
--final
--sealed
--strictfp
--native
--synchronized
--transient
--volatile
--native
--null
--finally

-permits

--super
--this

--const
--goto

zu recherchieren:
--var			???????
--module
	-exports
	-opens
	-requires
	-uses
	-provides
	-to
	-transitive
	-with


Konventionen:

interface-name: ____bitte (z.B. Zeichnenbitte)




*/